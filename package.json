{
  "name": "crm-torenaone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "docker:up": "docker compose up -d",
    "docker:ps": "docker ps",
    "docker:up:local": "DOCKER_CONFIG=.docker-dev /Users/mac-torenaone/Applications/Docker.app/Contents/Resources/bin/docker compose up -d",
    "docker:ps:local": "DOCKER_CONFIG=.docker-dev /Users/mac-torenaone/Applications/Docker.app/Contents/Resources/bin/docker ps",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "db:push": "npm --prefix backend run db:push",
    "db:seed": "npm --prefix backend run db:seed",
    "db:studio": "npm --prefix backend run db:studio"
  }
}
